module.exports = {
  content: [
    "./resources/**/*.blade.php"
  ],
  theme: {
    container: {
      center: true,
    },
    extends: {},
    // importamos la fuente y creamos una clase para usarla
    fontFamily: {
      'nunito': ['Nunito']
    }
  },
  plugins: [],
}
